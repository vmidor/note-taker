"use strict" 
/*****************************************************
 * Process
 *****************************************************/

let chai = require('chai');
let chaiAsPromised = require("chai-as-promised");
let should = chai.should();
let cmd = require("../lib/command.js");
let server = require("../index.js");

before(function(done) {
});

after(function(done) {
});

describe("1. Considering a command,", function() {
  it("  1.1. when, it's ran, a reference to the underlying process should be kept", function (done) {
    //Prepare
  });
})
